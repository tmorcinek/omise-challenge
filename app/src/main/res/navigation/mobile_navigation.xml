<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/mobile_navigation"
        app:startDestination="@+id/nav_charities">

    <fragment
            android:id="@+id/nav_charities"
            android:name="com.morcinek.omise.ui.charities.CharitiesFragment"
            android:label="@string/menu_charities"
            tools:layout="@layout/fragment_list"/>
    <fragment
            android:id="@+id/nav_donation"
            android:name="com.morcinek.omise.ui.donation.DonationFragment"
            android:label="@string/menu_donation"
            tools:layout="@layout/fragment_donation"/>

</navigation>